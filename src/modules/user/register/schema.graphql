# import Errors from "../../shared/Error.graphql"
# import Success from "../../shared/Success.graphql"

union RegisterResult = Success | Errors

type Mutation {
  register(fullName: String!, email: String!, password: String!): RegisterResult!
}
